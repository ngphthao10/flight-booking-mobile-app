<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/purple" /> <!-- Màu nền -->
    <corners android:radius="10dp" /> <!-- Góc bo tròn -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="4dp"
        android:bottom="4dp" />
    <stroke
        android:width="2dp"
        android:color="@color/purple" /> <!-- Viền nút -->
</shape>

